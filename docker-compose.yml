services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: carparty
      POSTGRES_PASSWORD: carparty
      POSTGRES_DB: carparty
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
